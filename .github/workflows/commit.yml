name: CommitComment

on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  commit_comments:
    runs-on: ubuntu-latest

    steps:
    - name: Commit
      env:
       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
         curl --request GET \
         --url https://api.github.com/repos/SiyaaJhawar/action/git/commits/b63d854b9c2d60ba843e7da1106c8076fe203704 \
         --header 'authorization: Bearer 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' \
         --header 'content-type: application/json' \
       
       
